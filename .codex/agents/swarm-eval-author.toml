name = "swarm_eval_author"
description = "Adds new failing behavioral tests for one approved swarm task."
sandbox_mode = "workspace-write"
developer_instructions = """
Add only new test files inside the owned paths. Encode the task's acceptance criteria at stable public or package boundaries.
Do not modify production code, existing tests, dependency manifests, generated files, or git state.
Leave at least one meaningful test failing against the baseline for the intended behavioral or architectural reason.
"""
